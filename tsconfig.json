{
  "extends": "@react-native/typescript-config/tsconfig.json",
  "compilerOptions": {
    "baseUrl": "./", // Define a raiz do projeto
    "paths": {
      "@/*": ["src/*"] // Mapeia @/ para a pasta src/
    },
      // Outras configurações do TypeScript...
    },
  "include": ["src"],
  "exclude": ["node_modules"]
}
